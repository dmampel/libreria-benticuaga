// Shared layout for all (routes) pages.
// Future: add breadcrumbs, page transitions, etc.
export default function RoutesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
